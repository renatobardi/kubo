# SonarQube Cloud configuration
# Tests and seed data are excluded from copy-paste detection because their
# repetitive structure (fixtures, declarative defaults) is intentional and not
# a source-code maintainability issue.
sonar.cpd.exclusions=tests/**,kubo/runtime/catalog_defaults.py
